# Shell aliases. Paste into ~/.zshrc or ~/.bashrc.
#
# Run these in a pane or tab other than the one Claude Code occupies.
#   ccl   pick from the session belonging to the current directory
#   ccll  pick from the most recently updated session, wherever it is

alias ccl='cclinks'
alias ccll='cclinks --latest'
